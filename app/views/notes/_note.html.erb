<%= turbo_frame_tag dom_id(note) do %>
  <div class="relative p-4 border rounded bg-white">
    <%= link_to '', edit_note_path(note), class: 'absolute top-0 left-0 right-0 bottom-0' %>
    
    <%= note.content %>
  </div>
<% end %>
